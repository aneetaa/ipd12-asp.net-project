@model IEnumerable<RecipeWorld.Models.Contact>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Messages</h2>
<br /><br />
<table class="table">
    <tbody>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FullName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Message)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PhoneNumber)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.FullName, new { @class = "text-field" })
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email, new {  @class = "text-field" })
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Message, new { @class = "text-field" })
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PhoneNumber, new { @class = "text-field" })
                </td>
            </tr>
        }
    </tbody>
    <tfoot>
        <tr>
            <td>
                @Html.ActionLink("Go back to contact page", "Create")
            </td>
            <td></td>
            <td></td>
            <td></td>
        </tr>
    </tfoot>
</table>
<br />
<p>
    
</p>
